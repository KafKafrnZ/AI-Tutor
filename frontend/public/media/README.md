# Ascend AI Media Assets

Current checked-in assets are optimized still images:

- `auth-bg.jpg`: dark hex-mesh auth background from the latest attachment.
- `hero-still.jpg`: sine-wave still for landing/mobile fallback.
- `hero-poster.jpg`: landing poster fallback.
- `tutor-ambient-poster.jpg`: ambient tutor background poster.

The reusable `VideoBackground` component will use MP4/WebM sources when they are added, but no video files were attached with this integration pass.
