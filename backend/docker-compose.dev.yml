version: '3.8'

services:
  backend:
    volumes:
      - .:/app  # Changed from ./backend to . since we are already in the folder
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload